<Window x:Class="Logger.Views.Settings"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel VerticalAlignment="Center">
        <Label Grid.Column="0" Grid.Row="6" Content="ログの保存場所:"/>
        <Button DockPanel.Dock="Right" Height="23" Content="..." Click="Button_Click"
            DataContext="{Binding LogPath, Mode=TwoWay}"/>
        <TextBox Height="24" Text="{Binding LogPath}"/>
    </DockPanel>
</Window>
